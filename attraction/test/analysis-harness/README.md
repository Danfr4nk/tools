# analysis-harness
Headless DOM test for the attraction-guide analysis layer (js/app.js).
Synthetic MediaPipe landmarks, real measureImage math, jsdom DOM.

Requires: `npm install jsdom` (node_modules is gitignored).

Run: `node --import ./register.mjs run.mjs`
